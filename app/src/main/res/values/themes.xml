<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Laostack" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- 스플래시 스크린 설정 -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/empty_drawable</item>
        <item name="postSplashScreenTheme">@style/Theme.Laostack</item>
    </style>
</resources>